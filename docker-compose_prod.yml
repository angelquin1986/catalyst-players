services:
  catalyst-api-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: catalyst_api_
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8082:8082"
    networks:
      - aq_files_mynet

networks:
  aq_files_mynet:
    external: true